{
  "name": "duck-app",
  "version": "1.0.0",
  "description": "Simpe duck-app for having fun",
  "main": "server.js",
  "scripts": {
    "start": "node server",
    "watch": "nodemon server",
    "heroku-postbuild": "cd react-ui/ && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "react-ui/node_modules"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/vincit/summmer-2018.git"
  },
  "license": "BSD-3-Clause",
  "author": "Vincit",
  "dependencies": {
    "body-parser": "1.15.2",
    "cors": "^2.8.1",
    "express": "4.14.0"
  },
  "devDependencies": {
    "nodemon": "^1.14.12"
  }
}
